# Frontend .dockerignore
# Keeps the build context lean — only src/, public/, package*.json
# and docker/ config are needed for the React build stage.

node_modules
build
.git
.gitignore
.env
.env.*
*.md
backend
docker/nginx

# Test & tooling artefacts
coverage
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
